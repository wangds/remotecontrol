# Getting Started

    uv sync [--without-dev]
    uv venv
    source .venv/bin/activate

# Install Service

Make package.

    uv build --sdist

Unpack into `~/service/remotecontrol` and make a `run` script.
